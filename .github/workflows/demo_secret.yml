name: Demo Secret

on:
  workflow_dispatch:
  
jobs:
  example:
    runs-on: ubuntu-latest
    steps:
      - run: echo "$MY_SECRET" == "TestSecret"
        env:
          MY_SECRET: ${{ secrets.MY_SECRET }}
